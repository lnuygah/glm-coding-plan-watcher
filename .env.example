# 环境变量覆盖（pydantic-settings）。复制为 .env 后填写；.env 不入库。
# 嵌套字段用双下划线分隔，前缀 GLM_WATCHER__
#
# 通知用的 webhook（敏感信息走 env，不要写进 config.yaml）：
# GLM_WATCHER__NOTIFY__WEBHOOK_URL=https://example.com/your/webhook
#
# 也可临时覆盖其它配置，例如关掉无头：
# GLM_WATCHER__HEADLESS=false
